{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Rust: Debug",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": ["build", "--bin=pure-rv32i"]
            },
            "args": ["input.txt"],
            "cwd": "${workspaceFolder}"
        }
    ]
}